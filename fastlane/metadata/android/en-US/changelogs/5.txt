Version 1.2.2

- Import multiple CSV files at once.
- 3D map keeps loading tiles while "Center on me" follows you (only while visible, to save data).
- Auto-reconnect now also retries when you turn Bluetooth back on.
- Android 15 support with true edge-to-edge (the dark border around the UI is gone).

Fixed: "connected but no packets" after airplane mode, tables going blank after long runs, Help not scrolling to the bottom, the import packet count, and several 2D-chart and 3D-map glitches.
