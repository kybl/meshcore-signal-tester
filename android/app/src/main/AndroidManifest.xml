<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Map tiles when online -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CSV export on Android 8–9 (API 26–28); not needed on API 29+ -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Bluetooth LE (Android 12+). neverForLocation: we never derive location
         from BLE scans (GPS has its own permission), which lets scanning work
         WITHOUT the location permission — needed for the "MeshCore device"
         packet-position source, where phone location isn't requested at all. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Bluetooth LE (Android 11 and below) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Location: foreground only. Capture runs in a foreground service of type
         "location" started while the app is visible, which may keep reading
         location with the screen off / app backgrounded without the all-the-time
         (background) location permission — and avoids Play's background-location
         policy review. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Normal (install-time) permission that keeps the connectedDevice FGS
         type eligible even when neither Bluetooth nor location permissions are
         held — e.g. a WiFi/TCP capture with packet positions sourced from the
         MeshCore device. Without an eligible prerequisite, startForeground
         with that type is rejected on Android 14+. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Foreground service that keeps BLE + GPS alive while the screen is off -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <!-- USB host mode for wired serial connections (optional — BLE still works without it) -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeshCore"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MeshcoreService"
            android:exported="false"
            android:foregroundServiceType="location|connectedDevice" />
    </application>
</manifest>
